import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Dashboard Header</h1>
            {/* Add navigation links or logo here */}
        </header>
    );
};

export default Header;